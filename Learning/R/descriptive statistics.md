# Basic statistics
- Single statistics:
mean(x) # Mean
sd(x) # standard deviation
min(x) # Minimum value

- Combined statistics:
fivenum(x) # Values for Tukey min,lower quartile, median,upper quartile,max
